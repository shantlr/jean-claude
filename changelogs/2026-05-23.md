- [feature] [changelog]
  - Added "Changelog" modal that opens automatically on startup when new changes land
  - Added command palette action so you can reopen changelog anytime via ⌘P -> "Changelog"
- [fix] [feed]
  - Fixed "waiting for permission" status getting stuck after approving permission or answering question
  - Fixed stacked feed task cards with transparent status rows overlapping text
- [fix] [new task]
  - Made step 2 work item thumbnails open preview modal when clicked
  - Added top-right X badge so you can remove attached image without reopening picker
- [feature] [agent]
  - Added animated gradient border around agent message area while agent actively runs
  - Improved OpenCode message streaming so thinking and text deltas update live during a turn
  - Completed OpenCode turns now show explicit result rows with correct replayed streaming state after reloads
- [improvement] [pr details]
  - Redesigned PR details page with 2-column layout and meta sidebar
  - Added inline CI expansion so you can inspect build timeline and logs without leaving page
  - Refined page header with breadcrumb navigation
- [feature] [run commands]
  - Added named run commands so task dropdowns and command log tabs can show friendly labels instead of raw scripts
  - Added reusable run groups that launch multiple commands in parallel and open separate log tabs per command
- [improvement] [run commands]
  - Combined commands and run groups into one draggable list in Project Settings > Run Commands so you can fully customize mixed ordering
  - Preserved that mixed ordering in the task run dropdown instead of forcing groups after commands
- [fix] [run commands]
  - Deleting a run command now rewrites only affected groups in same project instead of touching every group row
- [feature] [model presets]
  - Added global model presets so task and step composers can pick saved backend/model combinations from backend menu
  - Hid separate model selector when preset picker covers backend and model choice together
- [improvement] [settings]
  - Added preset-aware backend/model picker to AI generation slots and project override forms
  - Made saved backend/model combinations reusable across agent setup flows
- [improvement] [project settings]
  - Added preset-aware default coding agent selection so each project can prefill new tasks with saved backend/model combination
- [improvement] [notifications]
  - Made task desktop notifications open that task while keeping current Feed or project task view
  - Added status emoji to task notification titles for faster scanning
- [improvement] [task details]
  - Added Command Logs search so you can filter command tabs and visible log output while investigating runs
  - Scoped Command Logs ⌘F shortcut to panel itself and kept selected command stable while filtering
- [fix] [task details]
  - Fixed task message streams reopening near an old prompt instead of jumping to latest output when you switch focus to a task
- [improvement] [changelog]
  - Grouped each branch or work item into single changelog entry instead of stacking follow-up entries
  - Added scopes and concise nested bullets so changelog updates scan faster in modal
  - Increased changelog modal size so longer daily updates are easier to read without extra scrolling
  - Removed redundant type badge and made scope labels more prominent without increasing their size
- [fix] [merge commits]
  - Fixed OpenCode-backed squash merges falling back to generic "Squash merge branch ..." commit titles
  - Switched OpenCode commit generation to native structured output so custom merge and commit messages parse reliably
- [fix] [project detection]
  - Fixed Codex project detection skipping sessions whose metadata line is larger than 8 KB
- [feature] [calendar]
  - Added macOS Calendar meeting reminders in Settings > General so Jean-Claude can notify you before locally synced meetings start
  - Added a next-meeting button in header that shows your next macOS Calendar meeting and expands to an upcoming meetings list
- [improvement] [calendar]
  - Added setup guidance in Settings > General for syncing a Microsoft Outlook calendar into macOS Calendar
  - Expanded header meeting cards with details, a soon/live indicator, and an "Open in Calendar" action while showing more upcoming meetings
- [fix] [calendar]
  - Switched meeting reads to EventKit via macOS Calendar permissions so recurring calendar events appear more reliably in header list and reminders
