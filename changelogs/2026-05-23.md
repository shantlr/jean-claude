- [feature] Added "Changelog" modal — opens automatically on startup when there are new changes, or anytime via ⌘P → "Changelog"
- [fix] Fixed "waiting for permission" status in task feed not clearing after approving a permission or answering a question
- [fix] Updated New Task step 2 work item image thumbnails so clicking opens a preview modal and a top-right X badge removes the image.
- [feature] Added animated gradient border around the agent message area while the agent is actively running
- [improvement] Redesigned PR details page — 2-column layout with meta sidebar, inline CI expansion (click a check to see build timeline + logs without leaving the page), redesigned header with breadcrumb navigation
